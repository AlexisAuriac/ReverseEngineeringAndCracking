#include <stdio.h>
#include <string.h>

int main(int argc, char **argv) {
	if (argc != 2) {
		puts("Usage: ./simple_binary [key]");
	}

	char *input = argv[1];
	int input_size = strlen(input);

	int i = 0;

	while (i <= 11) {
		
	}
}
